---
name: 'publish'

on:  # yamllint disable-line rule:truthy
    push:
        branches: ['main']

jobs:
  publish:
    uses: 'Nostra-Inc/poc-iac-consumer/.github/workflows/publish-reusable.yaml@NOSTRAPROJ-145'
